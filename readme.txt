MARKER:
click on the map to set marker
re-click marker to display info window
double-click to delete marker 
can't add label (e.g. A, B, etc.) on top of the symbol


POLYLINE: (flight path)
automatically appears to connect markers 